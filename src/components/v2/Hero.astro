---
import BrowserMockup from './BrowserMockup.astro';
---

<section class="relative min-h-[110vh] bg-[#171e19] overflow-hidden flex flex-col items-center justify-center pt-24 pb-32">
  <!-- Background effects -->
  <div class="absolute inset-0">
    <!-- Grid overlay -->
    <div class="absolute inset-0 opacity-[0.03]" style="background-image: linear-gradient(rgba(255,255,255,0.5) 1px, transparent 1px), linear-gradient(90deg, rgba(255,255,255,0.5) 1px, transparent 1px); background-size: 60px 60px;"></div>
    <!-- Radial glow -->
    <div class="absolute top-1/4 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[800px] h-[800px] rounded-full bg-[#4d37f6] opacity-[0.08] blur-[120px]"></div>
    <div class="absolute bottom-0 left-1/2 -translate-x-1/2 w-[600px] h-[400px] rounded-full bg-[#7c3aed] opacity-[0.06] blur-[100px]"></div>
  </div>

  <!-- Content -->
  <div class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
    <!-- Badge -->
    <div class="inline-flex items-center gap-2 bg-[#4d37f6]/20 border border-[#4d37f6]/30 rounded-full px-4 py-1.5 mb-8">
      <span class="bg-[#4d37f6] text-white text-[10px] font-bold px-2 py-0.5 rounded-full uppercase tracking-wider">Nuevo</span>
      <span class="text-white/80 text-sm font-medium">IA para WhatsApp Business</span>
    </div>

    <!-- Headline -->
    <h1 class="text-5xl sm:text-6xl lg:text-7xl font-extrabold text-white tracking-tight leading-[1.1] max-w-4xl mx-auto mb-6">
      Tu negocio.
      <span class="bg-gradient-to-r from-[#4d37f6] via-[#7c3aed] to-[#a855f7] bg-clip-text text-transparent"> Una plataforma.</span>
    </h1>

    <!-- Subheadline with typing cursor -->
    <p class="text-white/60 text-lg sm:text-xl font-medium max-w-2xl mx-auto mb-10 leading-relaxed">
      El CRM todo-en-uno para e-commerce en Colombia. Pedidos, envíos, facturación, inventario y WhatsApp con IA<span class="typing-cursor">|</span>
    </p>

    <!-- CTAs -->
    <div class="flex flex-wrap justify-center gap-4 mb-6">
      <a
        href="https://app.binah.co/onboarding"
        class="bg-white text-[#171e19] font-bold text-base px-8 py-4 rounded-full shadow-[0_0_20px_rgba(77,55,246,0.3)] transition-all duration-300 hover:shadow-[0_0_40px_rgba(77,55,246,0.5)] hover:scale-105 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[#4d37f6]"
      >
        Comenzar Gratis
      </a>
      <a
        href="#funcionalidades"
        class="text-white font-bold text-base px-8 py-4 rounded-full border border-white/20 bg-white/5 backdrop-blur-sm transition-all duration-300 hover:bg-white/10 hover:border-white/30 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[#4d37f6]"
      >
        Ver Cómo Funciona
      </a>
    </div>

    <!-- Trust indicators -->
    <div class="flex items-center justify-center gap-6 text-white/40 text-sm font-medium mb-16">
      <span class="flex items-center gap-1.5">
        <svg class="w-4 h-4 text-[#4d37f6]" fill="currentColor" viewBox="0 0 20 20">
          <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd" />
        </svg>
        Sin tarjeta de crédito
      </span>
      <span class="flex items-center gap-1.5">
        <svg class="w-4 h-4 text-[#4d37f6]" fill="currentColor" viewBox="0 0 20 20">
          <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd" />
        </svg>
        14 días de prueba gratis
      </span>
    </div>

    <!-- Browser mockup -->
    <BrowserMockup />
  </div>
</section>

<style>
  .typing-cursor {
    animation: blink 1s step-end infinite;
    color: #4d37f6;
    font-weight: 700;
  }

  @keyframes blink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0; }
  }
</style>
