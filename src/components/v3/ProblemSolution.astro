---
const problems = [
  'Copias pedidos de Shopify a una hoja de Excel',
  'Generas guías de envío una por una en la página de la transportadora',
  'Facturas manualmente en Siigo cada pedido despachado',
  'Tu equipo pierde horas respondiendo las mismas preguntas en WhatsApp',
  'No tienes visibilidad real de tu inventario entre bodegas',
];

const solutions = [
  'Los pedidos de Shopify y WooCommerce llegan automáticamente',
  'Genera guías de Coordinadora y 99 Envíos con un clic',
  'Facturación automática en Siigo al despachar, con multi-credencial',
  'Chatbot IA en WhatsApp atiende el 70% de consultas sin intervención',
  'Inventario multi-bodega sincronizado en tiempo real',
];
---

<section class="bg-[#f8f9fa] py-20 lg:py-28">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-16">
      <h2 class="text-4xl lg:text-5xl font-bold tracking-tight text-slate-900 leading-[1.1] font-serif">
        Deja de improvisar tu operación
      </h2>
      <p class="mt-4 text-lg font-medium text-slate-500 max-w-2xl mx-auto leading-relaxed">
        Pasa de procesos manuales y desconectados a una operación automatizada e integrada.
      </p>
    </div>

    <div class="grid md:grid-cols-2 gap-6">
      <!-- Problem card -->
      <div class="bg-white border border-slate-200 rounded-2xl p-8">
        <h3 class="text-2xl font-semibold text-slate-400 mb-6 font-serif italic">Sin Binah...</h3>
        <ul class="space-y-4">
          {problems.map((item) => (
            <li class="flex items-start gap-3">
              <svg class="w-5 h-5 text-rose-400 shrink-0 mt-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
              </svg>
              <span class="text-slate-500 font-medium text-sm leading-relaxed">{item}</span>
            </li>
          ))}
        </ul>
      </div>

      <!-- Solution card -->
      <div class="relative">
        <div class="absolute -inset-1 bg-indigo-500 rounded-2xl blur-3xl opacity-[0.07]"></div>
        <div class="relative bg-gradient-to-br from-indigo-50 to-white border border-indigo-200 rounded-2xl p-8">
          <h3 class="text-2xl font-bold text-slate-900 mb-6 font-serif">Con Binah</h3>
          <ul class="space-y-4">
            {solutions.map((item) => (
              <li class="flex items-start gap-3">
                <svg class="w-5 h-5 text-indigo-500 shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
                  <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd" />
                </svg>
                <span class="text-slate-700 font-medium text-sm leading-relaxed">{item}</span>
              </li>
            ))}
          </ul>
        </div>
      </div>
    </div>
  </div>
</section>
