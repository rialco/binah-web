---
import BrowserMockup from './BrowserMockup.astro';
---

<section class="relative min-h-screen bg-[#0f172a] overflow-hidden flex flex-col items-center justify-center pt-24 pb-32">
  <!-- Background effects -->
  <div class="absolute inset-0">
    <!-- Grid overlay -->
    <div class="absolute inset-0 bg-grid-overlay"></div>
    <!-- Radial glow -->
    <div class="absolute top-1/4 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[800px] h-[800px] rounded-full bg-indigo-500 opacity-[0.07] blur-[120px]"></div>
    <div class="absolute bottom-1/4 left-1/2 -translate-x-1/2 w-[600px] h-[400px] rounded-full bg-purple-500 opacity-[0.05] blur-[100px]"></div>
  </div>

  <!-- Content -->
  <div class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
    <!-- Badge -->
    <div class="inline-flex items-center gap-2 bg-indigo-500/10 border border-indigo-500/20 rounded-full px-4 py-1.5 mb-8">
      <span class="text-indigo-400 text-sm font-medium font-display">NUEVO: WhatsApp IA 2.0</span>
    </div>

    <!-- Headline -->
    <h1 class="text-5xl sm:text-6xl lg:text-7xl xl:text-8xl font-bold text-white tracking-tight leading-[1.1] max-w-5xl mx-auto mb-6 font-serif">
      Tu e-commerce,
      <span class="bg-gradient-to-r from-indigo-400 via-purple-400 to-pink-400 bg-clip-text text-transparent"> una sola plataforma</span>
    </h1>

    <!-- Subheadline with typing cursor -->
    <p class="text-slate-400 text-lg sm:text-xl font-medium max-w-2xl mx-auto mb-10 leading-relaxed">
      El CRM todo-en-uno para e-commerce en Colombia. Pedidos, envíos, facturación, inventario y WhatsApp con IA<span class="typing-cursor">|</span>
    </p>

    <!-- CTAs -->
    <div class="flex flex-wrap justify-center gap-4 mb-6">
      <a
        href="https://app.binah.co/onboarding"
        class="bg-indigo-500 text-white font-medium text-base px-8 py-3.5 rounded-xl shadow-[0_0_20px_rgba(99,102,241,0.3)] transition-all duration-300 hover:bg-indigo-400 hover:shadow-[0_0_30px_rgba(99,102,241,0.5)] hover:scale-[1.02]"
      >
        Comenzar Gratis
      </a>
      <a
        href="#como-funciona"
        class="text-white font-medium text-base px-8 py-3.5 rounded-xl border border-white/10 bg-white/5 backdrop-blur-sm transition-all duration-300 hover:bg-white/10 hover:border-white/20"
      >
        Ver Cómo Funciona
      </a>
    </div>

    <!-- Trust indicators -->
    <div class="flex items-center justify-center gap-6 text-slate-500 text-sm font-medium mb-16">
      <span class="flex items-center gap-1.5">
        <svg class="w-4 h-4 text-indigo-400" fill="currentColor" viewBox="0 0 20 20">
          <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd" />
        </svg>
        Sin tarjeta de crédito
      </span>
      <span class="flex items-center gap-1.5">
        <svg class="w-4 h-4 text-indigo-400" fill="currentColor" viewBox="0 0 20 20">
          <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd" />
        </svg>
        14 días gratis
      </span>
    </div>

    <!-- Browser mockup -->
    <BrowserMockup />
  </div>
</section>

<style>
  .typing-cursor {
    animation: blink 1s step-end infinite;
    color: #6366f1;
    font-weight: 700;
  }

  @keyframes blink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0; }
  }
</style>
