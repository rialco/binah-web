---
const steps = [
  {
    number: '1',
    title: 'Conecta tu tienda',
    description: 'Integra Shopify o WooCommerce en menos de 10 minutos. Los pedidos se sincronizan automáticamente.',
    color: 'indigo',
    borderColor: 'border-indigo-500',
    glowColor: 'shadow-[0_0_15px_rgba(99,102,241,0.3)]',
    textColor: 'text-indigo-400',
  },
  {
    number: '2',
    title: 'Automatiza tu operación',
    description: 'Configura envíos, facturación y WhatsApp. Binah conecta todo y dispara acciones automáticas.',
    color: 'purple',
    borderColor: 'border-purple-500',
    glowColor: 'shadow-[0_0_15px_rgba(168,85,247,0.3)]',
    textColor: 'text-purple-400',
  },
  {
    number: '3',
    title: 'Escala tu negocio',
    description: 'Vende más sin contratar más. El chatbot IA atiende clientes mientras tú te enfocas en crecer.',
    color: 'pink',
    borderColor: 'border-pink-500',
    glowColor: 'shadow-[0_0_15px_rgba(236,72,153,0.3)]',
    textColor: 'text-pink-400',
  },
];
---

<section id="como-funciona" class="bg-[#0f172a] py-20 lg:py-28 relative overflow-hidden">
  <div class="absolute inset-0 bg-grid-overlay"></div>

  <div class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-16">
      <h2 class="text-4xl lg:text-5xl font-bold tracking-tight text-white leading-[1.1] font-serif">
        Cómo funciona
      </h2>
      <p class="mt-4 text-lg font-medium text-slate-400 max-w-2xl mx-auto leading-relaxed">
        Tres pasos para transformar tu operación de e-commerce.
      </p>
    </div>

    <div class="relative max-w-5xl mx-auto">
      <!-- Connecting line (desktop) -->
      <div class="hidden lg:block absolute top-[4.5rem] left-[16.67%] right-[16.67%] h-px bg-slate-700"></div>

      <div class="grid md:grid-cols-3 gap-8">
        {steps.map((step) => (
          <div class="glass-soft rounded-2xl p-8 text-center relative">
            <!-- Numbered circle -->
            <div class={`w-16 h-16 rounded-full border-2 ${step.borderColor} ${step.glowColor} flex items-center justify-center mx-auto mb-6 bg-[#0f172a] relative z-10`}>
              <span class={`text-2xl font-bold font-display ${step.textColor}`}>{step.number}</span>
            </div>
            <h3 class="text-xl font-semibold text-white mb-3 font-display">{step.title}</h3>
            <p class="text-slate-400 text-sm leading-relaxed">{step.description}</p>
          </div>
        ))}
      </div>
    </div>
  </div>
</section>
