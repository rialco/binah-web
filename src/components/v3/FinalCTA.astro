---

---

<section id="cta" class="relative bg-[#0f172a] py-20 lg:py-28 overflow-hidden">
  <!-- Background effects -->
  <div class="absolute inset-0">
    <div class="absolute inset-0 bg-grid-overlay"></div>
    <!-- Animated gradient glow -->
    <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[600px] h-[600px] cta-glow">
      <div class="absolute inset-0 bg-gradient-to-r from-indigo-500 via-purple-500 to-pink-500 rounded-full blur-3xl opacity-20"></div>
    </div>
  </div>

  <div class="relative z-10 max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
    <h2 class="text-4xl sm:text-5xl lg:text-6xl font-bold tracking-tight text-white leading-[1.1] font-serif">
      Lleva tu e-commerce<br />al siguiente nivel
    </h2>
    <p class="mt-6 text-xl text-slate-400 font-medium max-w-2xl mx-auto leading-relaxed">
      Únete a los negocios colombianos que ya automatizan su operación con Binah. Empieza tu prueba gratis hoy.
    </p>
    <div class="mt-10">
      <a
        href="https://app.binah.co/onboarding"
        class="inline-block bg-indigo-500 text-white font-medium text-lg px-10 py-4 rounded-xl shadow-[0_0_30px_rgba(99,102,241,0.4)] transition-all duration-300 hover:bg-indigo-400 hover:shadow-[0_0_50px_rgba(99,102,241,0.6)] hover:scale-[1.02]"
      >
        Comenzar Gratis
      </a>
    </div>
    <p class="mt-6 text-slate-500 text-sm font-medium">
      Sin tarjeta de crédito. 14 días de prueba gratis. Cancela cuando quieras.
    </p>
  </div>
</section>

<style>
  .cta-glow {
    animation: ctaPulse 6s ease-in-out infinite;
  }

  @keyframes ctaPulse {
    0%, 100% { transform: translate(-50%, -50%) scale(1); }
    50% { transform: translate(-50%, -50%) scale(1.1); }
  }
</style>
